<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ titulo }}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Inter', sans-serif; background-color: #f8f9fa; }
        .card { border: none; box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.15); border-radius: 12px; }
        .header-bg { background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: white; padding: 2rem 0; margin-bottom: 2rem; }
        .badge-positivo { background-color: #d1e7dd; color: #0f5132; }
        .badge-mejorar { background-color: #fff3cd; color: #856404; }
    </style>
</head>
<body>

    <header class="header-bg shadow-sm">
        <div class="container text-center">
            <h1 class="fw-bold">👨‍🏫 {{ titulo }}</h1>
            <p class="lead mb-0">Gestión de devoluciones en tiempo real</p>
        </div>
    </header>

    <main class="container">
        <div class="row g-4">
            <div class="col-lg-4">
                <div class="card p-4 sticky-lg-top" style="top: 20px;">
                    <h3 class="h5 mb-4 border-bottom pb-2">Nueva Observación</h3>
                    <form action="/guardar" method="POST">
                        <div class="mb-3">
                            <label class="form-label">Estudiante</label>
                            <select name="estudiante_id" class="form-select" required>
                                <option value="">Seleccionar...</option>
                                {% for est in estudiantes %}
                                    <option value="{{ est.id }}">{{ est.nombre }} ({{ est.comision }})</option>
                                {% endfor %}
                            </select>
                        </div>

                        <div class="mb-3">
                            <label class="form-label">Categoría</label>
                            <select name="categoria" class="form-select">
                                <option value="Lógica">Lógica</option>
                                <option value="Sintaxis">Sintaxis</option>
                                <option value="Participación">Participación</option>
                                <option value="Diseño">Diseño UI/UX</option>
                            </select>
                        </div>

                        <div class="mb-3">
                            <label class="form-label">Tipo de Feedback</label>
                            <div class="d-flex gap-2">
                                <input type="radio" class="btn-check" name="tipo_feedback" id="pos" value="positivo" checked>
                                <label class="btn btn-outline-success w-100" for="pos">Positivo ✅</label>
                                
                                <input type="radio" class="btn-check" name="tipo_feedback" id="mej" value="a_mejorar">
                                <label class="btn btn-outline-warning w-100" for="mej">A mejorar ⚠️</label>
                            </div>
                        </div>

                        <div class="mb-3">
                            <label class="form-label">Comentario</label>
                            <textarea name="comentario" class="form-control" rows="4" required placeholder="Escribe el feedback detallado..."></textarea>
                        </div>

                        <button type="submit" class="btn btn-primary w-100 fw-bold py-2">Guardar Registro</button>
                    </form>
                </div>
            </div>

            <div class="col-lg-8">
                <div class="card p-4">
                    <h3 class="h5 mb-4 border-bottom pb-2">Historial Reciente</h3>
                    <div class="table-responsive">
                        <table class="table table-hover align-middle">
                            <thead class="table-light">
                                <tr>
                                    <th>Estudiante</th>
                                    <th>Categoría</th>
                                    <th>Feedback</th>
                                    <th>Estado</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for f in feedbacks %}
                                <tr>
                                    <td>
                                        <div class="fw-bold">{{ f.estudiantes.nombre }}</div>
                                        <small class="text-muted">ID: #{{ f.id }}</small>
                                    </td>
                                    <td><span class="badge bg-secondary opacity-75">{{ f.categoria }}</span></td>
                                    <td><small>{{ f.comentario }}</small></td>
                                    <td>
                                        {% if f.tipo_feedback == 'positivo' %}
                                            <span class="badge badge-positivo">Positivo</span>
                                        {% else %}
                                            <span class="badge badge-mejorar">A mejorar</span>
                                        {% endif %}
                                    </td>
                                </tr>
                                {% else %}
                                <tr>
                                    <td colspan="4" class="text-center py-4">No hay registros aún.</td>
                                </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <footer class="py-5 text-center text-muted small">
        &copy; 2026 Gestor de Feedback - Sistema v1.0
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>